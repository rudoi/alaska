apiVersion: tekton.dev/v1alpha1
kind: ClusterTask
metadata:
  name: alaska-kubectl-executor
spec:
  inputs:
    params:
    - name: path
      type: string
    resources:
    - name: repo
      type: git
  steps:
  - name: dump-path
    image: ubuntu 
    command: ["cat"]
    args: ["$(inputs.resources.repo.path)/$(inputs.params.path)"]